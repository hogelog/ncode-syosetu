require "spec_helper"

describe Ncode::Syosetu::Polly do
  it "has a version number" do
    expect(Ncode::Syosetu::Polly::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
